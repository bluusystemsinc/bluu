var Bluu = (function () {
    "use strict";

    var set_alert = function (user_id, device_id, alert_id, duration, unit, text, email) {
        console.log(duration);
        console.log(unit);
        console.log(device_id);
    };

    return {
        set_alert: set_alert
    };

}());
